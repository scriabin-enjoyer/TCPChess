# frozen_string_literal: true

module MyGameServer
  module Protocol
    # Implements the SYSTEM-type protocol messages
    module System
      # echo req, reply
      # ping pong
      # bye
      # join game, queued, join success
      # game start
      # game end
      # game disconnect
      # Leave game
    end
  end
end
